---
import skills from "../data/skills.json"
---

<section id="skills" class="px-10 mb-36 md:px-20  flex flex-col">
    <h2 class="mb-12 text-3xl 2xl:text-5xl md:mb-20 md:mt-10 md:mx-auto md:text-center">Skills</h2>
    <div class="flex flex-col-reverse w-full md:flex-row">
        <div class="md:w-7/12 flex justify-center py-10 md:py-4 md:px-20">
            <ul class="flex flex-row flex-wrap justify-center gap-x-6 gap-y-4">
                {skills.map(skill => {
                    return(
                        <li style={`background-color: ${skill.bgColor}; color: ${skill.textColor};`} class="hover:scale-125 hover:shadow-lg transition-all duration-100 flex text-sm px-4 py-3 2xl:text-2xl 2xl:px-8 2xl:py-6 rounded-md">
                            <img class="w-5 h-5 mr-2 2xl:w-8 2xl:h-8 2xl:mr-3" src={skill.logo} alt=""/>
                            <span class="h-0">{skill.name}</span>
                            </li>
                        )
                })}
            </ul>
        </div>  
        <div class="md:w-6/12 flex justify-center md:justify-start items-center">
            <h2 class="text-5xl 2xl:text-8xl text-sky-600 text-center md:text-right">¿Que utilizaremos {<br/>} <span class="text-7xl 2xl:text-9xl font-bold">HOY</span>?</h2>
        </div>
    </div>
</section>